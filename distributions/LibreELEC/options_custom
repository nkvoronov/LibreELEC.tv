### CUSTOM OPTIONS ###

# Reset skins
  SKINS=""

# remove default skin included in Kodi (yes/no)
  SKIN_REMOVE_SHIPPED="no"
  
# Custom packages
  CUSTOM_SUPPORT="no"

# Custom source
  DISTRO_CUSTOM_SRC=""
  
# Locales
  LOCALES_SUPPORT="yes"

# Libxcb extention
  LIBXCB_EXT="yes"

# Enable shared build  
  ENABLE_SHARED="yes"

# additional drivers (DVB) to install (select one):

# Media build drivers (TBS)
  TBS_MEDIA_BUILD="no"

# Media build drivers (CrazyCat)
  CC_MEDIA_BUILD="no"

if [ "$TBS_MEDIA_BUILD" = yes ]; then
  ADDITIONAL_DRIVERS="$ADDITIONAL_DRIVERS tbs-media-build"
fi

if [ "$CC_MEDIA_BUILD" = yes ]; then
  ADDITIONAL_DRIVERS="$ADDITIONAL_DRIVERS cc-media-build"
fi

# KODI addons
  KODI_REPOSITORY_YELLOWDRAGON="no"

# Services
  LCDPROC_SERVICE="no"
  TRANSMISSION_SERVICE="no"
  OSCAM_SERVICE="no"

# TV Services
  TVHEADEND_SERVICE="no"
  VDR_SERVICE="no"

# Tools
  SYS_TOOLS_ENABLED="no"
  DVB_TOOLS_ENABLED="no"

# Perl
  PERL_VERSION="5.24.0"
  PERL_SUPPORT="no"

# Docker Fix
  DOCKER_FIX="no"
